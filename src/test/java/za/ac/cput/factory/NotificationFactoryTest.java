package za.ac.cput.factory;
import org.junit.jupiter.api.Test;
import za.ac.cput.domain.Notification;
import za.ac.cput.domain.User;
import za.ac.cput.util.Helper;
import static org.junit.jupiter.api.Assertions.*;
import java.time.LocalDate;
public class NotificationFactoryTest {

    @Test
    void testCreateNotification() {
        // Arrange
//            int notificationID = 1;
//            User user = new User(); // Assuming User has a no-args constructor; customize as needed.
//            user.setUserID(1);  // Set necessary user attributes
//            user.setUserName("John Doe"); // Assuming there's a username field
//            String message = "Your order has been shipped!";
//            LocalDate date = LocalDate.now();
//
//            // Act
//            Notification notification = NotificationFactory.createNotification(notificationID, user, message, date);
//
//            // Assert
//            assertNotNull(notification, "Notification object should not be null");
//            assertEquals(notificationID, notification.getNotificationID(), "Notification ID should match");
//            assertEquals(user, notification.getUserID(), "User should match");
//            assertEquals(message, notification.getMessage(), "Message should match");
//            assertEquals(date, notification.getDate(), "Date should match");
//        }

        // Additional tests can be added here for null or invalid input scenarios

    }


}